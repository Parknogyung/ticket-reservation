  server:
    port: 8081 # ??(?) ?? (?????? 8080? ? ?? ?? ???)

  spring:
    application:
      name: ticket-server

    # 1. ?????? ?? (Docker Compose? ???? ?)
    # 1. ?????? ?? (User environment: localhost:3636, root/1234)
    datasource:
      url: jdbc:mariadb://0.0.0.0:3306/ticket_db?createDatabaseIfNotExist=true
      username: root
      password: 1234
      driver-class-name: org.mariadb.jdbc.Driver

    # 2. JPA (ORM) ??
    jpa:
      hibernate:
        ddl-auto: update  # Changed to 'update' to preserve data
      show-sql: true
      properties:
        hibernate:
          format_sql: true
      open-in-view: false

    # 3. Redis ??
    data:
      redis:
        host: 0.0.0.0
        port: 6379
        password: 1234

    # 4. [???] Java 21/25 ?? ??? ??? (??? ??? ???)
    threads:
      virtual:
        enabled: true

  # 5. gRPC ?? ??
  grpc:
    server:
      port: 9090             # ?????? ??? ??? (static://localhost:9090)
      negotiation-type: PLAINTEXT # ?????? SSL ?? ?? ??

  # 6. ?? ?? ??
  logging:
    level:
      root: INFO
      com.server.portfolio: DEBUG # ?? ??? ???? ??
      org.hibernate.SQL: DEBUG      # ?? ???? ??